// import fs from 'fs';
// import path from 'path';

// export const generatePDFData = (fileName: string): string => {
//   const pdfData = fs.readFileSync(path.join('../assets/', fileName));
//   const encoded = pdfData.toString('base64');
//   return `data:application/pdf;base64,${encoded}`;
// }
